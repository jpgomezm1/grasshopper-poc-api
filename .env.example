# Database connection string (PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database

# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key for Whisper transcription
OPENAI_API_KEY=sk-...

# Environment (development | production)
ENVIRONMENT=development

# CORS Origins (comma-separated for production)
CORS_ORIGINS=https://your-frontend.netlify.app,http://localhost:5173

# JWT Secret (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=your-super-secret-key-change-this

# -----------------------------------------------------------------------------
# Storage (GH-S3-INFRA-04 · D-010 Supabase Storage)
# Leave empty in S3-S11 dev — storage_service.py falls back to in-memory stub.
# Real values land in S12 alongside Heroku/Netlify cutover.
# -----------------------------------------------------------------------------

# Backend choice: "supabase" | "stub" | "" (auto · uses supabase if SUPABASE_SERVICE_KEY is set)
STORAGE_BACKEND=

# Supabase project URL · format: https://<project-ref>.supabase.co
SUPABASE_URL=

# Supabase service_role key · server-side ONLY · never expose to FE
SUPABASE_SERVICE_KEY=

# Supabase Storage bucket · created in S12 (private bucket · signed URLs only)
SUPABASE_STORAGE_BUCKET=grasshopper-uploads

# -----------------------------------------------------------------------------
# Email transactional (GH-S7 · D-016 Resend default + stub fallback)
# Leave empty in S7-S11 dev · email_service.py falls back to stub backend
# (logs the send attempt and marks delivered=False · reason=no_provider_configured).
# Real provisioning lands in S12: domain + DKIM/SPF + Resend account.
# -----------------------------------------------------------------------------

# Resend API key · server-side ONLY · obtained from resend.com dashboard
RESEND_API_KEY=

# Sender identity used as the "From" header. Must match a verified domain on Resend.
EMAIL_FROM=Grasshopper <hola@grasshopper.co>

# -----------------------------------------------------------------------------
# Feature flags
# -----------------------------------------------------------------------------

# Clinical PDF generation (WeasyPrint + GTK). Set to `false` on Windows dev
# boxes that don't have libgobject/cairo/pango installed. When `true` and GTK
# is missing, the endpoint returns 503 with a clear message (see B-023 fix).
CLINICAL_PDF_ENABLED=true
